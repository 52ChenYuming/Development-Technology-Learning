/**
 * @param {number[]} prices
 * @return {number}
 */
var maxProfit = function(prices) {
    // //方法一：贪心法
    // // 思路：只在股票上涨时卖出，下跌时买入
    // let result = 0;
    // for (let i = 1; i < prices.length; i++) {
    //     result += Math.max(prices[i] - prices[i - 1], 0);
    // }
    // return result;

};